apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: val-api
  namespace: val-api
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: val-api
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
